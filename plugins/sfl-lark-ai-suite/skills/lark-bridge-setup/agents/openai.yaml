interface:
  display_name: "旧互換：Lark Bridgeセットアップ"
  short_description: "既存利用者向けの旧入口（新規はlark-setup）"
  default_prompt: "$lark-bridge-setup を使い、既存の呼び出し名でLark接続基盤を診断・設定してください。"
policy:
  allow_implicit_invocation: false
