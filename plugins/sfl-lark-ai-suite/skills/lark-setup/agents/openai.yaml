interface:
  display_name: "診断後のLark接続基盤導入"
  short_description: "診断結果から公式CLI・Bridge・議事録の不足分だけを導入"
  default_prompt: "$lark-setup を使い、完了済みの診断結果をもとにLark公式CLI・Bridge・議事録を不足分だけ順番に設定してください。"
policy:
  allow_implicit_invocation: false
