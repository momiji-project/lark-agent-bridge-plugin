interface:
  display_name: "Lark接続環境を再診断"
  short_description: "Plugin導入後に、PCを変更せず状態を再確認"
  default_prompt: "$lark-diagnose を使い、このPCのLark接続環境を変更せず再診断してください。導入や修復は行わないでください。"
policy:
  allow_implicit_invocation: true
